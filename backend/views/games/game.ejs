

<h1>Game</h1>

<div class="container">

        <div class="item container-sm" >
            <h1><%=game.title %></h1>
            
            <img src="<%=game.img %>" alt="" class=" img-fluid img-thumbnail ">
        </div>

        <div>
            <a href="<%=game.links.buy %>">BUY HERE</a>
            <br>
            <a href="<%=game.links.trailer %>">View Trailer: no link yet</a>
        </div>

        <br>

        <div class="container">
            <% for(let i=0; i < game.comments.length; i++){ %>
                <div>
                        <h1><%=game.comments[i] %></h1>
                </div>
            <% } %>     
        </div>

</div>